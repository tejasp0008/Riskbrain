# RiskBrain pinned dependencies
# Versions checked against PyPI on 2026-09-01 (Phase 0), numpy re-checked
# 2026-09-02 after a Python 3.13 install failure. Re-check before bumping;
# do not assume newer versions work without re-validating the build order
# in CLAUDE.md.
#
# NOTE: xgboost>=3.4.1 requires Python >=3.12. Use a Python 3.12+ venv.
# NOTE: several pins below are NOT current stable, because vllm==0.28.0
# constrains them transitively. Re-check all three whenever vllm is bumped.
#   numpy==2.4.6      vllm hard-pins numba==0.65.0, which requires
#                     numpy<2.5,>=1.22. The lower end matters too: numpy
#                     1.x has no Python 3.13 wheel (max is cp312), so the
#                     usable window on 3.13 is 2.1 through 2.4.x. 2.4.6 is
#                     the newest release satisfying both.
#   fastapi==0.136.3  vllm requires fastapi[standard]<0.137.0,>=0.133.0.
#   torch (unpinned)  vllm hard-pins torch==2.13.0 and its CUDA 13 stack;
#                     do not pin torch here or it will fight vllm.

# --- Inline serving path ---
vllm==0.28.0
transformers==5.16.1
bitsandbytes==0.50.2
fastapi==0.136.3
uvicorn[standard]==0.34.0

# --- Fine-tuning ---
peft==0.20.0

# --- Tabular fraud model ---
xgboost==3.4.1

# --- Retrieval ---
faiss-cpu==1.15.0
sentence-transformers==6.0.1

# --- Calibration / fusion ---
scikit-learn==1.5.2
numpy==2.4.6
pandas==2.2.3
matplotlib==3.11.1

# --- Observability ---
sqlalchemy==2.0.36
